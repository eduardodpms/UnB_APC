#include <stdio.h>

int main(){
    int n;
    scanf("%d", &n);
    printf("%d\n", (10-((((n%10)*2/10 + ((n%10)*2)%10) + ((n%100)/10) + (((n%1000)/100*2/10) + ((n%1000)/100*2)%10) + (n/1000))%10))%10);
    return 0;
}